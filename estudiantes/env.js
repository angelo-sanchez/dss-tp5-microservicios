const PORT = 3001;

const DB_PORT = 5432
const DB_HOST = "localhost"
const DB_USER = "postgres"
const DB_PASSWORD = "root"
const DB_NAME = "turni"

module.exports = {
    PORT,
    DB_PORT,
    DB_HOST,
    DB_USER,
    DB_PASSWORD,
    DB_NAME
}